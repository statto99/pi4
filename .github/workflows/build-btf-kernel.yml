name: Build RPi4 Kernel with BTF

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
      # 1. Checkout
      - name: Checkout
        uses: actions/checkout@v4

      # 2. Install dependencies
      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y \
            repo git curl \
            build-essential \
            clang llvm lld \
            flex bison bc \
            libssl-dev libelf-dev \
            dwarves pahole

      # 3. Initialize Android kernel source
      - name: Init kernel repo
        run: |
          mkdir android-kernel
          cd android-kernel

          repo init -u https://android.googlesource.com/kernel/manifest -b common-android16-6.12-lts

          mkdir -p .repo/local_manifests
          curl -L https://raw.githubusercontent.com/raspberry-vanilla/android_kernel_manifest/android-16.0/manifest_brcm_rpi.xml \
            -o .repo/local_manifests/rpi.xml

          repo sync -j$(nproc)

      # 4. Patch defconfig for BTF
      - name: Patch defconfig for BTF
        run: |
          cd android-kernel

          CONFIG=common/arch/arm64/configs/android_rpi4_defconfig
          
          # Create a backup and clean the config
          cp $CONFIG ${CONFIG}.backup
          grep -v "CONFIG_EXTRA_FIRMWARE" ${CONFIG}.backup > $CONFIG
          
          cat <<EOF >> $CONFIG

          # --- Enable debug info (required for BTF) ---
          CONFIG_DEBUG_INFO=y
          CONFIG_DEBUG_INFO_DWARF5=y

          # --- Enable BTF ---
          CONFIG_DEBUG_INFO_BTF=y

          # --- Must be disabled for BTF ---
          CONFIG_DEBUG_INFO_NONE=n
          CONFIG_DEBUG_INFO_REDUCED=n
          CONFIG_DEBUG_INFO_SPLIT=n

          # --- BPF core ---
          CONFIG_BPF=y
          CONFIG_BPF_SYSCALL=y
          CONFIG_BPF_JIT=y

          EOF

      # 5. Patch build.config
      - name: Patch build.config
        run: |
          cd android-kernel
          echo "DEBUG_INFO=1" >> common/build.config.rpi || true

      # 6. Build kernel
      - name: Build kernel
        run: |
          cd android-kernel
          tools/bazel build //common:rpi4 --verbose_failures

      # 7. Package boot files
      - name: Package boot files
        run: |
          cd android-kernel
          
          # Find all boot components
          KERNEL_IMAGE=$(find bazel-bin -name "Image" -type f | head -1)
          RAMDISK=$(find bazel-bin -name "ramdisk.img" -type f | head -1)
          RECOVERY_RAMDISK=$(find bazel-bin -name "ramdisk-recovery.img" -type f | head -1)
          DTB_FILES=$(find bazel-bin -name "*.dtb" -type f)
          
          # Create package directory
          mkdir -p android-boot-package
          
          # Copy files
          [ -n "$KERNEL_IMAGE" ] && cp "$KERNEL_IMAGE" android-boot-package/Image
          [ -n "$RAMDISK" ] && cp "$RAMDISK" android-boot-package/ramdisk.img
          [ -n "$RECOVERY_RAMDISK" ] && cp "$RECOVERY_RAMDISK" android-boot-package/ramdisk-recovery.img
          
          # Copy DTB files
          for dtb in $DTB_FILES; do
            cp "$dtb" android-boot-package/
          done
          
          echo "Package contents:"
          ls -la android-boot-package/

      # 8. Upload boot package
      - name: Upload boot package
        uses: actions/upload-artifact@v4
        with:
          name: android-rpi4-boot-files
          path: android-kernel/android-boot-package/
